# Sentiment Analyzer

A Flask web application that performs sentiment analysis on text using TextBlob.

## Deployment on Render
1. Push this repository to GitHub.
2. Connect the repository to Render.
3. Render will use `requirements.txt` to install dependencies and `Procfile` to run `gunicorn app:app`.
